<!DOCTYPE html>
<html>
  <head>
    <title>Calculation Result</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container-fluid mt-5">
      <h1 class="text-center mb-4">Calculation Result</h1>
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="card">
            <div class="card-body">
              <!-- Display the user's name -->
              <h4 class="card-title text-center">
                Hello, <%= firstName %> <%= lastName %>!
              </h4>
                <h5 class="card-subtitle mb-2 text-center">
                    You selected the operation: <%= operation %>
                </h5>

              <!-- Perform the operation and display the result -->
              <% if (operation === "+") { %>
              <h2 class="text-center">Addition</h2>
              <p class="text-center">
                <%= numOne %> + <%= numTwo %> = <%= parseInt(numOne) +
                parseInt(numTwo) %>
              </p>
              <% } else if (operation === "-") { %>
              <h2 class="text-center">Subtraction</h2>
              <p class="text-center">
                <%= numOne %> - <%= numTwo %> = <%= parseInt(numOne) -
                parseInt(numTwo) %>
              </p>
              <% } else if (operation === "*") { %>
              <h2 class="text-center">Multiplication</h2>
              <p class="text-center">
                <%= numOne %> * <%= numTwo %> = <%= parseInt(numOne) *
                parseInt(numTwo) %>
              </p>
              <% } else if (operation === "/") { %>
              <h2 class="text-center">Division</h2>
              <p class="text-center">
                <% if (parseInt(numTwo) !== 0) { %> <%= numOne %> / <%= numTwo
                %> = <%= parseInt(numOne) / parseInt(numTwo) %> <% } else { %>
                Division by zero is not allowed. <% } %>
              </p>
              <% } %>
              <!-- Button to go back to the input form -->
              <div class="text-center mt-4">
                <a href="/selection-input" class="btn btn-primary"
                  >Start Over</a
                >
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
